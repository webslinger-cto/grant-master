[build]
  base = "apps/frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Environment variables needed (set in Netlify dashboard):
# NEXT_PUBLIC_API_URL = https://grantsmaster.onrender.com/api/v1

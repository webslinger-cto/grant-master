[build]
  base = "apps/frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Environment variables — set these in the Netlify dashboard:
# NEXT_PUBLIC_API_URL = https://grantops-backend.onrender.com/api/v1
